<div class="table-responsive"><table class="data-table"><thead><tr><th>Alvo</th><th>Grupo</th><th>Valor</th><th>IP listado</th><th>RBL</th><th>Status</th><th>Primeira detecção</th><th>Última detecção</th><th>Resolução</th><th>Duração aproximada</th><th>Última resposta listed</th><th>Alertas</th></tr></thead><tbody>
@forelse($events as $event)
<tr><td>@if($event->target)<a href="{{ route('rbl.targets.show', $event->target) }}">{{ $event->target->name }}</a>@else — @endif</td><td>{{ $event->target?->group?->name ?? '—' }}</td><td>{{ $event->target?->value ?? '—' }}</td><td>{{ $event->last_checked_value ?? '—' }}</td><td>{{ $event->list?->name ?? '—' }}</td><td>{{ $event->status }}</td><td>{{ $event->first_seen_at?->format('d/m/Y H:i:s') ?? '—' }}</td><td>{{ $event->last_seen_at?->format('d/m/Y H:i:s') ?? '—' }}</td><td>{{ $event->resolved_at?->format('d/m/Y H:i:s') ?? '—' }}</td><td>{{ $event->durationMinutes() }} min</td><td>{{ $event->last_response ?? '—' }}</td><td>@include('rbl.alert-status')</td></tr>
@empty<tr><td colspan="12">Nenhum evento encontrado.</td></tr>@endforelse
</tbody></table></div>
